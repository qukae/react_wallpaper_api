import React from 'react';

export default function Error() { // i need to make proper error msg. it shows when server responds error
  return (
    <div className="Error">
      <p>Error</p>
    </div>
  );
}
